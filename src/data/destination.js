import caribbean from "./caribbean";
import europe from "./europe";
import tours from "./tours";

export default {
    destinations: {
        caribbean: caribbean,
        tours: tours,
        europe: europe
    },
};
